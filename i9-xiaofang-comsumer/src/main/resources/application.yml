spring:
  application:
    name: i9-xiaofang-comsumer
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
feign:
  hystrix:
    enabled: true
management:
  endpoint:
    health:
      show-details: always
